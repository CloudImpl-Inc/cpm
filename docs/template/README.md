### CPM template related commands

#### template fill

Prompt user for template placeholder values and fill them. Can run for single file or folder.

```bash
cpm template fill [options]
```

**Options:**
- `-f, --file <file>`: file name to fill (optional).